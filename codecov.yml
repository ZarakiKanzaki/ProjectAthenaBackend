secret:v1::tEojND4U5v5fHEXFTbev9xB121gtNK56mBF6oiLezqths5lomJ/7wxq8oWmEyO28cznhNyrVKZZRvmdJwAdMow==
comment:                  # this is a top-level key
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: false  # if true: only post the comment if coverage changes
  require_base: no        # [yes :: must have a base report to post]
  require_head: yes       # [yes :: must have a head report to post]
github_checks: true
coverage:
  status:
    patch: false
ignore:
  - "src/AthenaBackend.Infrastructure/Migrations/**/*" 
  - "src/AthenaBackend.Infrastructure/ReadModel/**/*map.cs" 
  - "src/AthenaBackend.Infrastructure/WriteModel/**/*map.cs" 
  - "src/AthenaBackend.Infrastructure/Migrations/*" 
